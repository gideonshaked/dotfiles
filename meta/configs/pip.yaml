- shell:
    - command: pip3 install -r bin/requirements.txt
      stdout: true
      description: Install dependencies for scripts in ~/bin

    - command: pip3 install -r manifest/tools-requirements.txt
      stdout: true
      description: Install dev tools with Pip
