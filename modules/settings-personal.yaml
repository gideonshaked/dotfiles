# Personal Claude settings: no work plugins.
#
# Listed first in the profile so the link exists before core's install steps
# read it.

- link:
    ~/.claude/settings.json:
      path: agents/claude/settings.personal.json
      force: true
