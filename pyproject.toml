[project]
name = "dotfiles"
version = "v0.0.0"
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=8"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
