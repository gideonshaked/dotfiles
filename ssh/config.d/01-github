Host github.com
	# The 1Password agent offers a read-only deploy key first, which breaks
	# pushes, so this host uses the key on disk.
	IdentitiesOnly yes
	IdentityAgent none
	IdentityFile ~/.ssh/id_ed25519_github
