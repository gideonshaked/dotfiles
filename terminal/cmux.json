{
  "$schema": "https://raw.githubusercontent.com/manaflow-ai/cmux/main/web/data/cmux.schema.json",
  "schemaVersion": 1,

  // `claude agents` only reads bypass from its own command line;
  // permissions.defaultMode in settings.json governs sessions started by hand
  // and never reaches the children the agent view dispatches.
  "actions": {
    "claudeAgents": {
      "type": "agent",
      "agent": "claude",
      "args": ["agents", "--dangerously-skip-permissions"],
      "title": "Claude agents",
      "newWorkspaceMenu": true
    }
  },

  "ui": {
    "newWorkspace": {
      "action": "claudeAgents"
    }
  },

  "notifications": {
    // The only class that blocks an agent, so it is never batched or delayed.
    "agentPermissionPrompt": true,
    // whenIdle: a finished agent is only worth interrupting for if nothing
    // else is already holding attention.
    "agentTurnComplete": "whenIdle",
    "agentIdleReminder": true,
    "unreadPaneRing": true,
    "dockBadge": true,
    "showInMenuBar": true,
    "sound": "default"
  },

  "sidebar": {
    "showAgentActivity": true,
    "showNotificationMessage": true,
    "notificationMessageLineLimit": 12,
    "showPullRequests": true,
    "showPorts": true,
    "showLog": true,
    "showProgress": true,
    "branchLayout": "vertical"
  },

  "app": {
    // Whatever needs a human rises to the top of the list on its own.
    "reorderOnNotification": true,
    "newWorkspacePlacement": "afterCurrent",
    // An agent is meant to outlive the pane it was started from, so a process
    // still running is not a reason to stop and ask before closing.
    "warnBeforeClosingTab": false,
    "confirmQuit": "never"
  },

  "terminal": {
    "autoResumeAgentSessions": true,
    "copyOnSelect": false,
    "showScrollBar": true
  },

  "browser": {
    "openTerminalLinksInCmuxBrowser": false,
    "interceptTerminalOpenCommandInCmuxBrowser": false
  },

  "automation": {
    "claudeCodeIntegration": true,
    // A subagent finishing is the orchestrator's business, not the user's.
    "suppressSubagentNotifications": true,
    "socketControlMode": "cmuxOnly",
    "workspaceAutoNaming": false
  }
}
